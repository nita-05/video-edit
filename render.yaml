services:
  - type: web
    name: vedit-backend
    env: python
    buildCommand: bash build.sh
    startCommand: cd backend && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
      - key: FLASK_APP
        value: app.py
    healthCheckPath: /api/health
